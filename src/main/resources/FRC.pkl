@ModuleInfo { minPklVersion = "0.27.0" }
module frc.robot.pkl.FRC

open class MotorController {
  id: Int32(isNonZero && isPositive)
  inverted: Boolean = false
}

open class PIDConstants {
  kP: Number
  kI: Number
  kD: Number
}
